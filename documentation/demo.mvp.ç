
@define const char 	*OSX = "OSX";
@define const char 	*platform = OSX;
@preprocess
{
	if (platform != OSX )
		exit(_(Unsupported platform.));
}
@undefine OSX
@undefine platform

#include <stdio.h>
//soon : @include <stdio.h>

int main()
{
	printf("%s\n", @preprocess { _(42); });
}
